services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: capsula
      POSTGRES_PASSWORD: capsula_dev
      POSTGRES_DB: capsula
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
